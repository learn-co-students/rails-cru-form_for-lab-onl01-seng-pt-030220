<h1> All Artists </h1>

<ul>
<div>
<% @artists.each do |artist| %> 
    <li><%= link_to artist.name, artist_path(artist) %> </li>
<% end %> 
</div>

</ul>

<%= link_to "Create an Artist", new_artist_path(@artist) %> 