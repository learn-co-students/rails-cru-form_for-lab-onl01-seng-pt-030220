<h1><%= link_to @song.name, song_path(@song) %></h1>

<p><%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %></p>

<p><%= link_to @song.genre.name, genre_path(@song.genre) if @song.genre%></p>