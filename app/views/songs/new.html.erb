<%= form_for (@song) do |f|%> 
<label> meow </label> 
<%= f.text_area :name %>
<label> Artist_id <label><br>
<%= f.text_area :artist_id %>
<label> Genre_id </label>
<%= f.text_area :genre_id %>

<%=f.submit%>
<%end%>